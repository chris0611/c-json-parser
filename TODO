# TODO

- Detect and warn about duplicate keys in objects
- Better memory management (currently reallocates too often)
- Better warnings and error handling in general
- Move lexing into its own file
- Make parser pure, i.e. thread-safe
- Improve build system (Maybe meson?)
- Separate driver code (main function and reading of command-line arguments)
into its own file(s)
- Documentation
- Testing
